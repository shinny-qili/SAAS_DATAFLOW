目录路径：
    一级：工作台
    二级：制单员指令

    左侧一个树结构，根目录是制单员工作台，一级目录包括，所有合同指令、我认领的指令
    右侧所有合同指令表
        筛选
        按钮
            认领/取消认领
            备注
        表格列
            认领状态：待认领/认领人
            发起人：
            发起时间
            指令内容更新时间
            保值方案：
            指令类型：
            合同号
            客商简称
            合同要求和匹配内容
            指令状态：待完成/撤销待处理/完成
            备注记录：
        认领/取消认领接口
            指令id：指令_id
            制单员id：人员_id
    右侧我认领的交易指令表
        筛选
        按钮
            取消认领
            创建合同
            备注
            完成合同创建
            完成指令
        表格列
            指令状态：待完成/撤销待处理/完成
            发起人：
            发起时间
            指令内容更新时间
            保值方案：
            指令类型：
            合同号
            客商简称
            其他合同信息
            合同要求和匹配内容
            合同创建状态：完成/待完成/null
            备注记录：
        匹配接口
            指令id：指令_id
            合同id：合同_id
            合同数量：decimal
        完成合同创建接口
            指令id：指令_id
        完成指令接口
            指令id：指令_id    