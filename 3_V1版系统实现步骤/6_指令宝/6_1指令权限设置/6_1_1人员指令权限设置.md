目录路径：
    一级：指令宝
    二级：报表
    三级：人员指令权限设置
人员指令权限表
    筛选
    按钮
        创建
        修改
        删除
        禁用/启用
    列表
        保值方案
        指令类型
        指令创建人
        制单员
        交易员
    创建修改人员指令权限界面
        指令创建人：multy_selectbox；
        指令类型id：selectbox
        保值方案id：selectbox
        其他的根据指令类型提供form
        交易员：multy_selectbox；角色限定；可选
    创建修改人员指令权限接口：
        人员指令权限id：人员指令权限_id
        指令创建人：json_list；人员_id
        指令类型id：指令类型_id
        保值方案id：保值方案_id
        制单员id：人员_id
        交易员id：人员_id
    禁用删除启用接口
        人员指令权限_id：int
        status：enable/disable/delete

注意：点价指令的处理有点特殊，如果指令发起人是业务经理角色，业务经理只能对合同字段“业务经理”是自己的合同发起点价。如果是指令员，则可以对保值方案下的所有合同发起点价